{{- if and .Values.apiServer.secretStore.enabled (gt (int .Values.apiServer.replicaCount) 0) }}
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: {{ .Release.Name }}-api-server-secrets
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "directai.labels" . | nindent 4 }}
    app.kubernetes.io/component: api-server
spec:
  provider: azure
  parameters:
    usePodIdentity: "false"
    useVMManagedIdentity: "false"
    clientID: {{ .Values.workloadIdentity.clientId | quote }}
    keyvaultName: {{ .Values.apiServer.secretStore.keyVaultName | quote }}
    tenantId: {{ .Values.apiServer.secretStore.tenantId | quote }}
    objects: |
      array:
        {{- range .Values.apiServer.secretStore.secrets }}
        - |
          objectName: {{ .objectName }}
          objectType: secret
        {{- end }}
  secretObjects:
    - secretName: {{ .Release.Name }}-api-server-env
      type: Opaque
      data:
        {{- range .Values.apiServer.secretStore.secrets }}
        - objectName: {{ .objectName }}
          key: {{ .secretKey }}
        {{- end }}
{{- end }}
