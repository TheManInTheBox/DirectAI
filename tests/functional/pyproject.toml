[project]
name = "directai-functional-tests"
version = "0.1.0"
description = "Functional tests for live DirectAI deployments"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["."]
